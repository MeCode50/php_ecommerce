<?php

// Enable all error reporting
//error_reporting(E_ALL);

// Force display of errors directly in the script
//ini_set('display_errors', 1);
//ini_set('display_startup_errors', 1);

// Force the changes, even if overridden by php.ini
//ini_set('log_errors', 0);  // Make sure errors aren't just logged but displayed

//phpinfo();

require('contact.php');
